# Main password file for authentication
mosquitto_password_file /etc/flashmq/passwd

# Basic MQTT listener on port 1883
listen {
    protocol mqtt
    port 1883
}

# Default settings
allow_anonymous false
